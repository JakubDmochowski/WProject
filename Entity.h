#pragma once

#include <memory>

#include "Renderable.h"

class Entity {
    RenderablePtr renderable;

    public:
        Entity();
        ~Entity();

    /// TO DO
    /*
    Collision* collision;
    Hookable* hookable;
    Animation* animation;
    Physics* physics;
    AI* behavior;
    */
    ///
};
